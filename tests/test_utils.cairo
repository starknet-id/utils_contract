use array::ArrayTrait;
use result::ResultTrait;

// #[test]
// fn test_increase_balance() {
//     let contract_address = deploy_contract('utils', @ArrayTrait::new()).unwrap();

//     let mut calldata = ArrayTrait::new();
//     calldata.append(123.into());
//     calldata.append(0.into());

//     let result_before = call(contract_address, 'sum_balances', calldata).unwrap();
//     assert(*result_before.at(0_u32) == 0, 'Invalid balance');
// }
